# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Deploy to Firebase Hosting on merge
'on':
  push:
    branches:
      - main
jobs:
  build_and_deploy:
    runs-on: ubuntu-latest
    env: 
      apiKey:  ${{ secrets.APIKEY }}
      authDomain:  ${{ secrets.AUTHDOMAIN }}
      projectId:  ${{ secrets.PROJECTID }}
      storageBucket:  ${{ secrets.STORAGEBUCKET }}
      messagingSenderId:  ${{ secrets.MESSAGINGSENDERID }}
      appId:  ${{ secrets.APPID }}
      measurementId:  ${{ secrets.MEASUREMENTID }}
    steps:
      - uses: actions/checkout@v2
      - run: echo "${{env.apiKey}} ${{env.authDomain}} ${{env.projectId}} ${{env.storageBucket}} ${{env.messagingSenderId}} ${{env.appId}} ${{env.measurementId}}"
      - run: npm ci && npm run build
      - uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: '${{ secrets.GITHUB_TOKEN }}'
          firebaseServiceAccount: '${{ secrets.FIREBASE_SERVICE_ACCOUNT_PORTFOLIO_39AF8 }}'
          channelId: live
          projectId: portfolio-39af8
